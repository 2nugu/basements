"""basements_ros2 — Bridge between MPMSolver and myCobot hardware.

Phase-1 scope: offline-coupled validation (record → replay → diff).
Phase-3 scope: runtime-coupled (requires GPU or adaptive MPM).
"""

__version__ = "0.1.0"
